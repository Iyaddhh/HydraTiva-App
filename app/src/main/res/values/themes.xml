<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HydraTiva" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorButtonNormal">@color/endcolor</item>

    </style>

    <style name="Theme.HydraTiva" parent="Base.Theme.HydraTiva" />
    
    <style name="SlideActivityAnimation" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/slide_in_right</item>
        <item name="android:activityOpenExitAnimation">@anim/slide_out_left</item>
    </style>

    <style name="GradientButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/gradient_color</item>
        <item name="backgroundTint">@null</item>
    </style>

    <style name="NavBar" parent="Widget.MaterialComponents.FloatingActionButton">
        <item name="android:backgroundTint">@color/primary</item>
        <item name="shapeAppearanceOverlay">@style/FAB_CircularShape</item>
    </style>
</resources>